{
  "name": "gadt-compiler",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "test:demo": "npm run build && npm run start",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  }
}
