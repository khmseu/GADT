{
  "name": "gadt-compiler",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && npm run start",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
